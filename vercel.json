{
  "buildCommand": "npx prisma generate && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/admin/sync-products",
      "schedule": "0 6 * * *"
    }
  ]
}
